import RedirectLayout from "@/components/template/RedirectLayout";
import type { FC } from "react";

type Props = {};

const Redirect: FC<Props> = () => {
  return (
    <>
      <RedirectLayout />
    </>
  );
};
export default Redirect;

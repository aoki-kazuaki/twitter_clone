# Docker使用手順書
このTwitterCloneApplicationでは、ディレクトリ構成において、モノレポ管理を採用したものになっています。
実際に開発環境を実施する際には基本的にDockerを使用します。

## 実行コマンド一覧
プロジェクトのリポジトリクローン後に以下のコマンドを実行することで、フロントエンド(React)、バックエンド(SpringBoot)、DBの起動が可能になります。
※ ローカルPC上にDockerDesktopがインストールされており、かつ起動中である状態で以下のコマンドを実行すること
```
docker compose up
```